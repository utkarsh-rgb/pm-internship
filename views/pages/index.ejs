<div class="container my-3 text-center text-md-start">
  <h5 id="welcomeText" class="h3 fw-bold text-primary mb-1">Welcome Guest</h5>
  <p class="small text-muted mb-3">We’re glad to have you here</p>
</div>

<!-- Hero Section -->
<section class="hero position-relative" style="background: linear-gradient(135deg, #FF9933 0%, #FFFFFF 35%, #138808 100%); min-height: 70vh;">
  <div class="position-absolute top-0 start-0 w-100 h-100" style="background: rgba(0,0,0,0.05);"></div>
  <div class="container position-relative py-4 d-flex align-items-center min-vh-50">
    <div class="row align-items-center">
      <div class="col-lg-7 text-center text-lg-start">
        <span class="badge bg-success fs-6 px-3 py-1 mb-2 d-inline-block">
          <i class="bi bi-flag-fill me-1"></i>भारत सरकार | Government of India
        </span>
        <h1 class="h2 fw-bold text-dark mb-3" style="text-shadow: 1px 1px 2px rgba(255,255,255,0.7);">
          PM Internship Scheme
          <div class="text-primary fs-5 mt-1">प्रधानमंत्री इंटर्नशिप योजना</div>
        </h1>
        <p class="small text-dark mb-3" style="text-shadow: 1px 1px 2px rgba(255,255,255,0.7);">
          Empowering the youth of India through opportunities.<br>
          Connecting students across rural, tribal, and urban regions with meaningful internships.
        </p>

        <!-- Smaller Buttons -->
        <div class="d-flex flex-column flex-sm-row gap-2 justify-content-center justify-content-lg-start">
          <a href="/form" class="btn btn-success btn-sm px-3 py-1 fw-bold shadow-sm" style="min-width: 120px;">
            <i class="bi bi-rocket-takeoff-fill me-1"></i>Start
          </a>
          <a href="/about" class="btn btn-outline-dark btn-sm px-3 py-1 fw-semibold shadow-sm" style="min-width: 120px;">
            <i class="bi bi-info-circle-fill me-1"></i>Learn
          </a>
        </div>
      </div>
      <div class="col-lg-5 text-center mt-3 mt-lg-0">
        <img src="https://upload.wikimedia.org/wikipedia/commons/5/55/Emblem_of_India.svg" 
             alt="Government of India Emblem" 
             class="img-fluid mb-2" 
             style="max-height: 300px; filter: drop-shadow(0 5px 10px rgba(0,0,0,0.2));">
      </div>
    </div>
  </div>
</section>
<!-- Image Carousel Section -->
<div class="carousel-container" style="position: relative; width: 100%; max-width: 600px; margin: auto; overflow: hidden; border-radius: 10px;">
  
  <!-- Images -->
  <img src="/images/gov3.jpg" class="carousel-image active" style="width:100%; position:absolute; top:0; left:0; opacity:1; transition: opacity 0.5s;">
  <img src="/images/gov2.jpg" class="carousel-image" style="width:100%; position:absolute; top:0; left:0; opacity:0; transition: opacity 0.5s;">
  <img src="/images/gov1.jpg" class="carousel-image" style="width:100%; position:absolute; top:0; left:0; opacity:0; transition: opacity 0.5s;">
  <!-- Add more images if needed -->

  <!-- Navigation Buttons -->
  <button class="prev" style="position:absolute; top:50%; left:10px; transform:translateY(-50%); background:rgba(0,0,0,0.5); color:white; border:none; font-size:18px; padding:8px; border-radius:50%; cursor:pointer;">&#10094;</button>
  <button class="next" style="position:absolute; top:50%; right:10px; transform:translateY(-50%); background:rgba(0,0,0,0.5); color:white; border:none; font-size:18px; padding:8px; border-radius:50%; cursor:pointer;">&#10095;</button>
</div>

<script>
  const images = document.querySelectorAll('.carousel-image');
  const prevBtn = document.querySelector('.prev');
  const nextBtn = document.querySelector('.next');
  let current = 0;

  function showImage(index) {
    images.forEach((img, i) => {
      img.style.opacity = i === index ? '1' : '0';
    });
  }

  prevBtn.addEventListener('click', () => {
    current = (current - 1 + images.length) % images.length;
    showImage(current);
  });

  nextBtn.addEventListener('click', () => {
    current = (current + 1) % images.length;
    showImage(current);
  });

  // Auto-slide every 3 seconds
  setInterval(() => {
    current = (current + 1) % images.length;
    showImage(current);
  }, 3000);
</script>


<!-- Statistics Section -->
<section class="py-4 bg-white">
  <div class="container">
    <div class="text-center mb-4">
      <h2 class="h4 fw-bold text-primary mb-1">Transforming India's Youth</h2>
      <p class="small text-muted">Making a real impact across the nation</p>
    </div>
    <div class="row g-3 text-center text-sm-start">
      <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 p-3">
          <div class="text-success mb-2"><i class="bi bi-people-fill fs-3"></i></div>
          <h5 class="fw-bold text-primary mb-0">50,000+</h5>
          <p class="small text-muted mb-0">Students Registered</p>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 p-3">
          <div class="text-warning mb-2"><i class="bi bi-building-fill fs-3"></i></div>
          <h5 class="fw-bold text-primary mb-0">2,500+</h5>
          <p class="small text-muted mb-0">Partner Organizations</p>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 p-3">
          <div class="text-info mb-2"><i class="bi bi-geo-alt-fill fs-3"></i></div>
          <h5 class="fw-bold text-primary mb-0">28</h5>
          <p class="small text-muted mb-0">States & UTs Covered</p>
        </div>
      </div>
      <div class="col-6 col-md-3">
        <div class="card border-0 shadow-sm h-100 p-3">
          <div class="text-danger mb-2"><i class="bi bi-award-fill fs-3"></i></div>
          <h5 class="fw-bold text-primary mb-0">95%</h5>
          <p class="small text-muted mb-0">Success Rate</p>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Call to Action -->
<section class="py-4 text-white text-center" style="background: linear-gradient(135deg, #138808 0%, #FF9933 100%);">
  <div class="container">
    <h3 class="fw-bold mb-2">Ready to Shape Your Future?</h3>
    <p class="small mb-3">Join thousands of students already benefiting from the PM Internship Scheme</p>
    <a href="/form" class="btn btn-light btn-sm px-3 py-1 fw-bold shadow-sm">
      <i class="bi bi-arrow-right-circle-fill me-1"></i>Apply Now
    </a>
  </div>
</section>

<style>
@keyframes rotate {
  from { transform: translate(-50%, -50%) rotate(0deg); }
  to { transform: translate(-50%, -50%) rotate(360deg); }
}
.min-vh-50 { min-height: 50vh; }
</style>

<script>
document.addEventListener("DOMContentLoaded", () => {
  const studentName = localStorage.getItem("studentName");
  if (studentName) {
    document.getElementById("welcomeText").textContent = "Welcome " + studentName;
  }
});
</script>
